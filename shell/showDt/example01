#描述同example1

#/bin/sh

#unset删除指定的变量或者函数
unset num;

while [ "$num" != "end" ]
do
    echo "------------"
    echo "Please input a new num:"
    read num
    if [ "$num" = "end" ]
    then
       echo "I am a break"
       break
    fi
    echo "number is $num"
done
